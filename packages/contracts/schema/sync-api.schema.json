{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://palhatch.local/schemas/sync-api.schema.json",
  "title": "SyncApiContracts",
  "type": "object",
  "additionalProperties": false,
  "$defs": {
    "SyncPairRequest": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "device_name", "platform"],
      "properties": {
        "code": {
          "type": "string",
          "pattern": "^[A-Za-z2-9]{4}[- ]?[A-Za-z2-9]{4}$"
        },
        "device_name": { "type": "string", "minLength": 1, "maxLength": 80 },
        "platform": {
          "type": "string",
          "enum": ["linux-x64", "win32-x64"]
        },
        "app_version": {
          "type": ["string", "null"],
          "minLength": 1,
          "maxLength": 40,
          "default": null
        }
      }
    },
    "SyncPairResponse": {
      "type": "object",
      "additionalProperties": false,
      "required": ["device_id", "device_token", "api_base_url"],
      "properties": {
        "device_id": { "type": "string", "format": "uuid" },
        "device_token": { "type": "string", "minLength": 47, "maxLength": 128 },
        "api_base_url": { "type": "string", "format": "uri", "maxLength": 500 }
      }
    },
    "SyncHeartbeatRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "app_version": {
          "type": ["string", "null"],
          "minLength": 1,
          "maxLength": 40,
          "default": null
        },
        "status": {
          "type": "string",
          "enum": ["ok", "unchanged", "idle", "error"],
          "default": "ok"
        }
      }
    },
    "SyncPairingCodeResponse": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "expires_at"],
      "properties": {
        "code": { "type": "string", "pattern": "^[A-Z2-9]{4}-[A-Z2-9]{4}$" },
        "expires_at": { "type": "string", "format": "date-time" }
      }
    },
    "SyncServerMember": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "player_id",
        "nickname",
        "level",
        "guild_name",
        "world_name",
        "discriminator",
        "is_bound",
        "is_current_user"
      ],
      "properties": {
        "player_id": { "type": "string", "format": "uuid" },
        "nickname": { "type": "string", "minLength": 1, "maxLength": 120 },
        "level": { "type": ["integer", "null"] },
        "guild_name": { "type": ["string", "null"], "maxLength": 160 },
        "world_name": { "type": "string", "minLength": 1, "maxLength": 160 },
        "discriminator": { "type": "string", "pattern": "^#[0-9a-f]{6}$" },
        "is_bound": { "type": "boolean" },
        "is_current_user": { "type": "boolean" }
      }
    },
    "SyncDevice": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "name",
        "platform",
        "token_prefix",
        "app_version",
        "world_id",
        "last_seen_at",
        "last_snapshot_at",
        "revoked_at",
        "created_at",
        "members"
      ],
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "name": { "type": "string" },
        "platform": { "type": "string" },
        "token_prefix": { "type": "string" },
        "app_version": { "type": ["string", "null"] },
        "world_id": { "type": ["string", "null"], "format": "uuid" },
        "last_seen_at": { "type": ["string", "null"], "format": "date-time" },
        "last_snapshot_at": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "revoked_at": { "type": ["string", "null"], "format": "date-time" },
        "created_at": { "type": "string", "format": "date-time" },
        "members": {
          "type": "array",
          "items": { "$ref": "#/$defs/SyncServerMember" }
        }
      }
    },
    "SyncBindingInvitationCreated": {
      "type": "object",
      "additionalProperties": false,
      "required": ["invitation_path", "expires_at"],
      "properties": {
        "invitation_path": {
          "type": "string",
          "pattern": "^/(zh|en)/account/binding-invitations/[A-Za-z0-9_-]{43}$"
        },
        "expires_at": { "type": "string", "format": "date-time" }
      }
    },
    "SyncBindingInvitationPlayer": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "player_id",
        "nickname",
        "level",
        "guild_name",
        "discriminator"
      ],
      "properties": {
        "player_id": { "type": "string", "format": "uuid" },
        "nickname": { "type": "string", "minLength": 1, "maxLength": 120 },
        "level": { "type": ["integer", "null"] },
        "guild_name": { "type": ["string", "null"], "maxLength": 160 },
        "discriminator": { "type": "string", "pattern": "^#[0-9a-f]{6}$" }
      }
    },
    "SyncBindingInvitationPreview": {
      "type": "object",
      "additionalProperties": false,
      "required": ["device_name", "world_name", "expires_at", "players"],
      "properties": {
        "device_name": { "type": "string", "minLength": 1, "maxLength": 80 },
        "world_name": { "type": "string", "minLength": 1, "maxLength": 160 },
        "expires_at": { "type": "string", "format": "date-time" },
        "players": {
          "type": "array",
          "items": { "$ref": "#/$defs/SyncBindingInvitationPlayer" }
        }
      }
    },
    "SyncBindingInvitationAccepted": {
      "type": "object",
      "additionalProperties": false,
      "required": ["player_id"],
      "properties": {
        "player_id": { "type": "string", "format": "uuid" }
      }
    },
    "SyncClaimablePlayer": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "player_id",
        "nickname",
        "level",
        "guild_name",
        "world_name",
        "discriminator"
      ],
      "properties": {
        "player_id": { "type": "string", "format": "uuid" },
        "nickname": { "type": "string" },
        "level": { "type": ["integer", "null"] },
        "guild_name": { "type": ["string", "null"] },
        "world_name": { "type": "string" },
        "discriminator": { "type": "string" }
      }
    }
  }
}
