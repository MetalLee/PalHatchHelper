# PalHatchHelper 工程协作约定

## 项目目标

第一版是“帕鲁配种协作工作台”，不是通用服务器监控平台。核心闭环是安全同步库存、计算确定性配种路线、比较方案并由玩家人工推进计划。

## 必读文档

正式设计规格是唯一需求来源。计划用于约束交付顺序，不能覆盖规格。

每次启动只读下面的**核心**部分；其余按功能域**按需阅读**（见按需表），不进入启动必读。涉及新需求、跨模块改动或功能边界不明确时，应补读相关按需部分，不得凭核心部分猜测细节。

### 核心（每次启动必读）

`docs/superpowers/specs/2026-07-13-palworld-breeding-system-design.md`：

- 头部：修订状态与元信息。
- §1–§16：背景范围、核心决策、总体架构、目录设计、数据模型、权限与 RLS、存档同步、ParserAdapter、游戏目录、确定性配种算法、综合评分、AIProvider、任务状态机、我的计划、方案可复现。
- §18–§23：服务器进程与部署、错误处理、测试要求、第一版验收标准、后续扩展方向、设计一致性结论。

`docs/superpowers/plans/2026-07-13-palworld-breeding-system-implementation.md`：

- 头部：修订状态。
- Phase 8：管理员功能、部署和端到端验收。
- 2026-07-31 Catalog 2.0、物品库存与递归配方修订（实施中，含 Windows x64 扩展交付顺序）。
- 文末跨阶段变更规则。

### 按需阅读（任务涉及对应功能域时）

| 功能域 | 阅读位置 | 触发场景 |
|---|---|---|
| 前端产品设计 | 规格 §17 | 修改页面、组件、路由、交互、文案或可访问性 |
| 公共 Sync CLI 与安装体验 | 规格 §24、§30、§32 | 修改 sync/CLI、设备配对、上传、心跳或世界发现 |
| 品牌、公开首页与 SEO | 规格 §25–§29、§33–§36 | 修改 Landing、SEO、登录页、轮播或公开导航 |
| 账号绑定与邀请 | 规格 §37 | 修改账号页、绑定、换绑或邀请流程 |
| Catalog 2.0 物品库存与递归配方 | 规格 §31 | 修改物品库存、产量计算或九类目录 |
| 历史阶段实施细节 | 计划 Phase 0–7 与已完成修订索引 | 回溯阶段实现方式、验证命令或已批准依赖（如 `next-intl`） |

## 安全边界

禁止：

- 修改 `/opt/palworld`，包括其 Compose、配置、容器和文件权限。
- 修改、修复或编辑真实存档；源存档只能只读检查并复制。
- 访问生产密钥、生产数据库或真实服务器凭证。
- 提交 `.env` 或任何真实秘密信息。
- 开放新的公网端口；Agent 健康接口仅绑定 `127.0.0.1:18765`。
- 修改现有 Palworld Compose，或停止、重启 Palworld 和 mihomo 容器。
- 让 AI 生成配种事实、改变配方合法性或基础算法评分。
- 未运行相关测试就声称完成。
- 未经批准增加重量级生产依赖。
- 设置系统全局代理或 Docker daemon 全局代理。
- 向 `/opt/services/palworld-manager` 部署、执行生产部署或推送远程仓库，除非对应阶段获得明确批准。

## 开发工作流

固定使用：

```text
检查现状
→ 写失败测试或检查
→ 运行并确认失败
→ 最小实现
→ 运行局部测试
→ 运行全部验证
→ 检查 Git diff
→ 输出报告
```

失败测试必须验证真实行为，不以无意义断言制造失败。已有有效实现和用户未提交改动必须保留。

## 代码规范

- TypeScript 开启严格模式。
- Python 使用完整类型标注。
- 文件保持职责单一。
- 前后端业务字段来自共享 JSON Schema 或 OpenAPI。
- 不在 TypeScript 和 Python 中手工维护两份不一致的 DTO。
- 外部依赖通过 Adapter 隔离。
- 时间统一使用 UTC 存储和带时区的 ISO 8601 表达。
- 标识符使用稳定的英文内部 ID，中文只用于显示。
- 错误使用稳定错误码，不依赖错误文本判断。
- 配种关系和路线由固定数据版本与确定性算法生成；AI 仅解释和辅助排序。

## 验证要求

验证按变更影响范围执行，并避免重复运行同一检查：

- 开发过程中优先运行最小相关测试或检查，快速验证当前改动；扩大验证范围只在影响边界不明确或局部验证不足时进行。
- 完成前对最终代码状态执行一次覆盖受影响范围的验证。跨前端、Agent 或共享契约的改动优先使用根目录 `pnpm check`；仅影响单一包或单一组件时，使用对应的 lint、format、typecheck、test 和 build 命令。
- 聚合命令已成功覆盖的检查不得再单独重复执行；仅补充聚合命令未覆盖且与本次改动相关的验证。最后一次相关改动后已有成功结果的命令无需重跑。
- 纯文档、注释或不影响运行行为的配置说明改动，只执行针对性内容检查和 `git diff --check`，无需运行无关的 lint、typecheck、test 或 build。
- 若工具缺失，必须说明缺失工具、受影响验证、替代检查和安装方式，不能静默跳过。
